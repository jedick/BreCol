fit_classifier:
  results_json_template: results/{features}/{name}.json
  experiments:
    - name: cancer_diagnosis_baseline
      overrides:
        task: cancer_diagnosis
        model: baseline

    - name: cancer_diagnosis_knn
      overrides:
        task: cancer_diagnosis
        model: knn

    - name: cancer_diagnosis_svm
      overrides:
        task: cancer_diagnosis
        model: svm

    - name: cancer_diagnosis_random_forest
      overrides:
        task: cancer_diagnosis
        model: random_forest

    - name: cancer_type_baseline
      overrides:
        task: cancer_type
        model: baseline

    - name: cancer_type_knn
      overrides:
        task: cancer_type
        model: knn

    - name: cancer_type_svm
      overrides:
        task: cancer_type
        model: svm

    - name: cancer_type_random_forest
      overrides:
        task: cancer_type
        model: random_forest

# Flat list merged shallowly over the defaults.yaml run_uc_cap_pipeline baseline (feature sets; --feat 1..N).
run_uc_cap_pipeline:
  - { n_uc: 1000, n_clusters: 2000, n_cap: 5000 }
  - { n_uc: 2000, n_clusters: 2000, n_cap: 5000 }
  - { n_uc: 1000, n_clusters: 5000, n_cap: 5000 }
  - { n_uc: 2000, n_clusters: 5000, n_cap: 5000 }
  - { n_uc: 1000, n_clusters: 2000, n_cap: 10000 }
  - { n_uc: 2000, n_clusters: 2000, n_cap: 10000 }
  - { n_uc: 1000, n_clusters: 5000, n_cap: 10000 }
  - { n_uc: 2000, n_clusters: 5000, n_cap: 10000 }
  
train_hyenadna:
  results_json_template: results/hyenadna/{name}.json
  # Current practical probe set (optimize for immediate use).
  # Keep prior experiment rows for cumulative comparison across copied results.
  experiments:
    - name: ct_next6e_fp32_b6_sb_control_seed0
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        random_seed: 0
        amp: false
        grad_clip_norm: 1.0
    - name: ct_next6e_fp32_b5_sb_control_seed0
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 5
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        random_seed: 0
        amp: false
        grad_clip_norm: 1.0
    - name: ct_next6e_fp32_b6_sb_classwt_seed0
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        random_seed: 0
        amp: false
        grad_clip_norm: 1.0
        class_weight: balanced
    - name: ct_next6e_fp32_b6_sb_classwt_seed1
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        random_seed: 1
        amp: false
        grad_clip_norm: 1.0
        class_weight: balanced
    - name: ct_next6e_fp32_b6_sb_classwt_seed2
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        random_seed: 2
        amp: false
        grad_clip_norm: 1.0
        class_weight: balanced
    - name: ct_next6e_ampbf16_b6_sb_classwt_seed0
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        random_seed: 0
        amp: true
        amp_dtype: bfloat16
        grad_clip_norm: 1.0
        class_weight: balanced
    - name: ct_next10e_fp32_b6_sb_classwt_seed0
      overrides:
        task: cancer_type
        epochs: 10
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        random_seed: 0
        amp: false
        grad_clip_norm: 1.0
        class_weight: balanced
    - name: ct_stab6e_fp32_b6_sb_classwt_lr5e5_seed0
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        learning_rate: 5.0e-5
        random_seed: 0
        amp: false
        grad_clip_norm: 1.0
        class_weight: balanced
    - name: ct_stab6e_fp32_b6_sb_classwt_lowlr_warmup_seed0
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: warmup_cosine
        warmup_epochs: 1
        min_lr_ratio: 0.2
        learning_rate: 5.0e-5
        random_seed: 0
        amp: false
        grad_clip_norm: 1.0
        class_weight: balanced
    - name: ct_stab6e_fp32_b6_sb_classwt_lowlr_warmup_seed1
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: warmup_cosine
        warmup_epochs: 1
        min_lr_ratio: 0.2
        learning_rate: 5.0e-5
        random_seed: 1
        amp: false
        grad_clip_norm: 1.0
        class_weight: balanced
    - name: ct_temp6e_fp32_b6_sb_sqrtwt_seed0
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        random_seed: 0
        amp: false
        grad_clip_norm: 1.0
        class_weight: balanced_sqrt
    - name: ct_temp6e_fp32_b6_sb_sqrtwt_seed1
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        random_seed: 1
        amp: false
        grad_clip_norm: 1.0
        class_weight: balanced_sqrt
    - name: ct_temp6e_fp32_b6_sb_sqrtwt_freeze2_blr01_seed0
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        random_seed: 0
        amp: false
        grad_clip_norm: 1.0
        class_weight: balanced_sqrt
        freeze_backbone_epochs: 2
        backbone_lr_mult: 0.1
    - name: ct_temp6e_fp32_b6_sb_sqrtwt_freeze2_blr01_seed1
      overrides:
        task: cancer_type
        epochs: 6
        max_length: 4096
        num_sets: 5
        batch_size: 6
        tuning_metric: val_roc_auc
        train_sampler: study_balanced
        lr_schedule: none
        random_seed: 1
        amp: false
        grad_clip_norm: 1.0
        class_weight: balanced_sqrt
        freeze_backbone_epochs: 2
        backbone_lr_mult: 0.1
